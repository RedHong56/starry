services:
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    # 소스 변경 시 컨테이너 재시작 없이 반영 (개발용)
    volumes:
      - ./app:/app/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
